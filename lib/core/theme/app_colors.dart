import 'package:flutter/material.dart';

/// Collection of color constants used throughout the application.
///
/// Centralizing colors in a single class makes it easy to adjust the
/// application's color scheme from one place. The primary green is
/// derived from the example UI design provided.
class AppColors {
  static const primaryGreen = Color(0xFF0B7D3B);
  static const darkGreen = Color(0xFF075C2B);
  static const lightGreen = Color(0xFFE7F3EB);
  static const bannerBg = Color(0xFFF2F5F7);
}